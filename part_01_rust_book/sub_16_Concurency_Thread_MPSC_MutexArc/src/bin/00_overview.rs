/*
"Concurent programming" means different parts of a program execute ``independently``.
"Parallel programming" means different parts of a program execute ``at the same time``.

Rust's ownership and type systems are powerful set of tools
to help manage memory safety and concurrency problems!

By leveraging ownership and type checking,
many concurrency errors are compile-time errors in Rust rather than runtime errors.
=> All the errors are supervised at compile-time, making run-time more safe
=> Fearless Concurency

Note: in this guide, "concurent" and "parallel" are interchangeable
*/

fn main() {}
